# 工单：四族颜色对齐原型图 + 服装主色修正（v5）

> 里程碑：M1 补充 v5 | 执行：子 agent（MiniMax M3） | 验收：主 agent + owner 目检
> owner 原话："服装部分人格是什么颜色，服装就是什么颜色。衣服主色调必须是对应颜色的。"

## 颜色基准（owner 提供的原型图）

`C:/Users/19802/Desktop/R-C.png`（16personalities 官方 16 人格图，2048×2831 原图）。四族基准色以这张图为准：

- 第 1 行 = 分析家（紫），第 2 行 = 外交家（绿），第 3 行 = 守护者（蓝绿/teal），第 4 行 = 探险家（金黄）
- 参考值（16p 官方品牌色，需以实图采样为准微调）：紫 ≈ #88619A、绿 ≈ #33A474、蓝 ≈ #4298B4、黄 ≈ #E4AE3A

## 任务

### 1. 采样四族基准色并更新 palette.json（v4）

- 写脚本对 R-C.png 分行采样：每行 4 个人物的服装区域主色（取人物躯干区出现频次最高的饱和色），输出 4 个 hex
- 以采样值更新 `assets/style/palette.json` 的 families：main = 采样值；shadow/light/highlight 由 main 程序化推导（shadow = main 明度 × 0.55，light = × 1.35，highlight = × 1.7，封顶 255；推导后目检合理性可微调）
- 保持 JSON 结构不变（families/main/shadow/light/highlight + personalities + neutrals + outline）
- 同步适配 check_palette.py / pixelate.py（如果色值变了脚本逻辑不用动，但确认校验仍通过）

### 2. 修正 16 张形象图（assets/art/portraits/）

逐张审计"服装主色调 = 本族基准色，且足够显著"：

- **服装色正确但深浅不对的**（旧的紫偏深、绿偏暗等）：程序化重映射——把服装区旧 main/shadow/light/highlight 映射到新基准色推导的四档，毛色、描边、背景不动
- **服装被其他颜色稀释的**（owner 点名 ISFJ、ISTJ 的内搭问题；逐张检查同类问题）：image-01 重新生成，prompt 里写死"wearing solid <基准色 hex> outfit, entire outfit is <颜色名>, no inner layer of other colors"，再走量化管线
- 毛色规范不变（避开四族色，用 neutrals）；背景纯白；512×512 不变
- 修正后的产出仍放 `assets/art/portraits/`，候选存 `assets/art/concepts/v5/`

### 3. 校验与交付

- 逐张校验：全部颜色在色板内；服装区主色 = 本族 main（写脚本统计每张服装区像素色值分布，main 必须是最多的彩色）
- 同族 4 张服装 main 像素级一致
- 16 张全部通过后重拼 `assets/art/portrait-sheet.png`
- 交付报告 `docs/tech/M1-颜色对齐-v5-交付报告.md`：采样得到的 4 个基准色 hex、每张是"重映射"还是"重生成"、没把握的（诚实）

## 约束

- 中文注释（R9）；不做 git commit；不动 PRD/REVIEW/ISSUES/plan.md、resources/sprites/、工单文件
- 重生成某只超过 5 次仍不行的，标注"建议即梦出稿"，不拿差的凑数
