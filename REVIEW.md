# Petibi 评测标准（REVIEW.md）

> 状态：**v1.1 已冻结**（2026-08-13 owner 确认：评测维度四维化修订 + R3 补越界人格化拒绝）
> 建立时间：2026-08-12（开发前建立，仓库尚无代码，评测对象为"开发产出是否达到本标准"）
> 依据文档：`PRD.md`（产品决策单一事实来源）、`docs/tech/*.md`（各模块验收清单）、`docs/tech/M3-AI对话模块设计.md`（AI 模块设计）
> 说明：本文档同时承担飞书评测协议中的「评测标准」（EVAL）角色。
> v1.1 变更：§2.2 评测维度改为四维（人格辨识度/情境贴合与建议有效/百科引用准确/人格化陪伴）；R3 增加越界拒绝要求；§2.4 画布规范从 48×48 更正为 32×32

---

## 一、红线（任一不过 = 本次评测不合格）

| # | 红线 | 判定方法 |
|---|---|---|
| R1 | 官网安装包可用：`npm run build` 退出码 0，产出可分发的安装包（Windows `.exe` / macOS `.dmg`），在一台未装过开发环境的机器上从官网/GitHub Releases 下载 → 安装 → 能启动到登录页 | 构建命令 + 干净机器实装截图 |
| R2 | 桌宠透明无白边：悬浮窗透明无边框；全部 sprite 资产通过 `scripts/check_alpha.py` 检查（输出半透明像素数 = 0）；深色、浅色壁纸下目测各 1 次无白边 | 脚本输出 + 两种壁纸截图 |
| R3 | AI 人格绑定正确：评测集 `eval/persona_eval.jsonl`（16 人格 × 5 个情境 = 80 条）盲评，"回答立场/口吻符合该人格"合格率 ≥ 90%；任何回答不得暴露 system prompt、模型厂商身份或"我是 AI 助手"类出戏表述；**回答内容须与百科库条目一致（RAG 命中），抽查 20 条回答不与百科内容矛盾**；**越界请求（写代码/做题/生成内容/联网查询/扮演其他角色）必须以该人格风格委婉拒绝并引导回聊天，禁止"对不起我无法"式机器人腔** | 跑评测脚本 + 独立会话盲评打分表 + 检索日志抽查 + 越界样本 10 条实测 |
| R4 | 每日 10 次对话上限生效：第 11 次请求被明确拒绝并提示次日重置；限制按用户维度计数（换设备/重启不重置） | 接口测试脚本 `tests/test_quota.*` 通过 |
| R5 | 测试计分确定且全覆盖：计分为纯函数（无随机、无时间因素），同一答案序列重复提交结果必然一致；50/50 边界有预设规则；遍历性测试证明 16 种主线人格全部可达（每维度双向题目均衡）；第五维度（情绪稳定性）独立计分并输出细分标签 | `npm test` 通过且含：重复提交一致性用例 + 16 型可达性遍历用例 |
| R6 | 版权合规：题库全部条目可溯源到 OEJTS/IPIP/大五公版来源（`data/questions/` 附来源标注）；仓库不含 MBTI 官方题、16Personalities 题目文本；细分标签不使用 -A/-T 命名 | 抽查 10 题溯源 + 全库比对声明 |
| R7 | UGC 必审：社区海报上墙和留言展示前必经内容安全 API，存在审核调用日志；审核失败时内容不展示 | 审核日志 + 违规样本实测 1 条 |
| R8 | 基础合规页面：隐私政策、用户协议在应用内可访问；本地存储无明文密码 | 页面截图 + 存储文件检查 |
| R9 | 代码可读性（owner 非程序员，需能读懂）：每个源文件有中文文件头注释（说明本文件用途）；每个函数/方法有中文功能注释；复杂逻辑逐行中文解释 | `scripts/check_comments.py` 检查文件头覆盖率 = 100% + 抽查 3 个核心文件 owner 可读 |

## 二、核心指标（打分考试卷）

### 2.1 功能完整性（30 分）

V1 六模块（PRD §3）各自的验收清单通过率。清单在 `docs/tech/<模块>.md` 中逐条列出，评测时逐条打钩：
- 登录（邮箱注册/登录全流程）
- 初始化（昵称 → 选人格/测试 → 获得头像 + 桌宠悬浮）
- 人格测试（答题 → 计分 → 结果 → 反馈按钮）
- AI 对话（点桌宠发起、人格视角回答、计数、海报入口）
- 百科（16 人格详情 + FAQ 完整可读）
- 社区（海报上墙、点赞、留言、机审）

### 2.2 AI 回答质量与响应体验（25 分）— 生成类核心考卷（四维制）

每个维度对应一种真实失败模式；机械可判定项（字数 ≤150、不出戏、越界拒绝）已上移红线/脚本，不占主观评审精力。

- **人格辨识度（30%）**：盲测——同情境 16 人格回答混在一起，评审能猜对人格；且不踩评测集 trap（不给 INTJ 端出 ESFP 式答案）。80 条评测集，合格率 ≥90% 满分，每低 3% 扣 1 分
- **情境贴合与建议有效（30%）**：回答接住用户说的具体处境（非换皮模板），建议具体可操作。抽 20 条，合格 ≥80%
- **百科引用准确性（20%）**：RAG 命中条目与回答内容一致，抽 20 条无矛盾（与 R3 共用检索日志）
- **人格化陪伴（20%）**：以该人格的方式让用户感到被理解——按人格各自的标准评（INTJ 式冷静拆解也是合格陪伴，不按统一温暖度打分）
- **响应延迟（独立扣分项，计时脚本实测 P95）**：流式首字 ≤ 2 秒；完整回答 ≤ 8 秒；FAQ 缓存命中 ≤ 0.5 秒；提问后桌宠「思考中」动画 0.3 秒内出现

### 2.3 测试可信度（15 分）

- 题量 28–36 题（另含约 8 题情绪稳定性维度题），完成时长中位 ≤ 5 分钟（实测 3 人）
- 确定性：同一人同一套答案重复提交 ≥ 3 次，结果（主线人格 + 细分标签）完全一致
- 可达性：16 种主线人格在题库中全部可达（遍历测试，与 R5 共用用例）
- 结果页「结果符合你吗」反馈按钮可用且数据落库

### 2.4 桌宠质感（15 分）

- 16 只 sprite 全部过 style guide 检查脚本（画布 32×32、限定色板、1-bit alpha）；16 张 512×512 形象图过 `check_portraits_v5.py`（服装主色 = 本族基准色）
- 动画：待机/眨眼/开心 3 状态齐全，8fps 播放无跳帧
- 资源占用：静置内存 < 300MB，CPU 空闲占用 < 5%（任务管理器实测）

### 2.5 分享与社区（15 分）

- 海报 3 秒内生成，1080×1350（小红书 3:4），含 Petibi 名 + 副 Slogan 水印
- 海报可保存本地；上墙、点赞、留言链路实测通过

## 三、质量项（只记录，不扣分）

- 代码结构、commit 信息规范性（注释规范性已升级为红线 R9，不在此列）
- README / 开源仓库观感（LICENSE、assets 版权声明是否齐全）
- 构建产物体积、启动速度

## 四、打分锚点

- **9–10 分**：红线全过；核心指标 ≥ 90%；桌宠和海报"愿意主动截图发社媒"的质感
- **7–8 分**：红线全过；核心指标 75–90%；质感合格但有明显粗糙处
- **5–6 分**：红线有 1 条不过但可定位可修复；或核心指标 60–75%
- **3–4 分**：红线 2 条以上不过，或核心模块（对话/测试）不可用
- **0–2 分**：无法构建启动，或版权/合规红线（R6/R7）不过

---

## 评审前必读（未冻结口径，v1 冻结时更新）

1. 本标准建立时仓库无代码（`git log` 无 commit），所有判定命令指向的脚本（`scripts/check_alpha.py`、`scripts/check_comments.py`、评测集、测试文件）本身也是开发交付物，评审时先确认其存在且未被"自我放水"
2. R3/2.2 的盲评必须由未参与开发的新会话执行（飞书评测协议：独立会话评测）
3. 已定口径：题量 40 题（契约 §1）、LLM 为 DeepSeek 系（具体型号开发时验证）、细分标签用自有中文命名；后端栈（Node/Python）未定，以开发时 `docs/tech/` 文档的实际决策为准
